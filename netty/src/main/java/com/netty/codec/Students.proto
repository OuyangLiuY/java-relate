

syntax = "proto3";// 版本
option java_outer_classname = "studentPOJO"; // 生成的外部类名,同时也是文件名
//protobuf 使用message 管理数据
message Student{  //会在 外部类生成一个内部类 Student 他死真正发送的POJO对象
  int32 id = 1; // Student 类中一个属性名 为id 类型 为int 32 (protobuf类型) 1 表示属性的序号,不是值
  string name = 2;

}